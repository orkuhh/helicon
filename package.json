{
  "name": "helicon",
  "private": true,
  "version": "0.17.0",
  "description": "Helicon - open-source desktop + web ADE for Meta's Muse Code CLI",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
