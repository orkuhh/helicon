{
  "name": "@helicon/server",
  "version": "0.15.0",
  "description": "Helicon local server: REST + SSE bridge between the shared UI and Muse MSP hosts",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "helicon-server": "dist/cli.js",
    "helicon-browser": "dist/src/browserCli.js"
  },
  "scripts": {
    "build": "npm run build --workspace @helicon/browser && npm run build --workspace @helicon/daemon && tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@harjjotsinghh/aonia": "^0.1.0",
    "@helicon/browser": "*",
    "@helicon/daemon": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
