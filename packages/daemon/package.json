{
  "name": "@helicon/daemon",
  "version": "0.15.0",
  "description": "Helicon daemon: MSP host management, session orchestration, local state, WSL routing",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace @helicon/browser && tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@harjjotsinghh/aonia": "^0.1.0",
    "@helicon/browser": "*",
    "@muse-code/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
