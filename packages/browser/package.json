{
  "name": "@helicon/browser",
  "version": "0.15.0",
  "description": "Helicon in-app browser engine: Chromium via Playwright, CDP automation, profiles",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "playwright-core": "^1.51.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
